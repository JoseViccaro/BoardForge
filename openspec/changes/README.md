# OpenSpec — Change Proposals (`openspec/changes/`)

Active change proposals and SDD RFCs are tracked in this directory.
When completed and merged into the main specifications and codebase, changes are archived to `openspec/changes/archive/`.
